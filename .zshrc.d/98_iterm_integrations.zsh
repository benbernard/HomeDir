# if [[ $IS_REMOTE_INSTANCE != "true" ]]; then
#   export ITERM_ENABLE_SHELL_INTEGRATION_WITH_TMUX=1
#   test -e "${HOME}/.iterm2_shell_integration.zsh" && source "${HOME}/.iterm2_shell_integration.zsh"
# fi
