# Installing: (not meant to be run as a script)

Try copying locally and running:
bin/mac/setupMac.sh

# Old Instructions

  git clone git://github.com/benbernard/HomeDir.git
  cd HomeDir
  git submodule init
  git submodule update
  cd ..
  rsync -av HomeDir/ .


... I should really setup a drop script to do this



